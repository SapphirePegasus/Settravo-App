// app/(tabs)/_fab.tsx
export default function FabPlaceholder() { return null; }

/**
 * The center FAB tab references a screen name _fab which must exist as a 
 * file even though it never renders (the tabBarButton override intercepts 
 * all presses).
 */